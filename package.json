{ 
  "scripts": {
  "test": "set NODE_OPTIONS=--experimental-vm-modules && npx jest --coverage --forceExit",
  "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jest": "^29.1.2",
    "nodemon": "^2.0.20",
    "supertest": "^6.3.0"
  }
}
